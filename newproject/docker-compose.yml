version: '3.9'

services:
  web:
    build: .
    ports:
      - "8888:8888"
    volumes:
      - .:/app
    environment:
      - DEBUG=True
    restart: always  # Ensures the container restarts if it crashes
